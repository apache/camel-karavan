apiVersion: camel.apache.org/v1
kind: Integration
metadata:
  name: test
spec:
  flows:
    - route:
        from:
          uri: direct1
          steps:
            - wireTap:
                uri: direct:direct2
                setHeader:
                  - name: header1
                    expression:
                      simple:
                        expression: ${header.demo}
                  - name: header2
                    expression:
                      simple:
                        expression: ${body}
